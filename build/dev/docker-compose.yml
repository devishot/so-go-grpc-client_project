version: '3.6'

services:
  api:
    image: so-client_project-dev:latest
    env_file:
      - ../../default_env/database.env
    ports:
      - 8080:8080
    volumes:
      - ../../:/go/src/github.com/devishot/so-go-grpc-client_project